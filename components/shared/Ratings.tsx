import React from "react";

type Props = {
  image: string | null;
  name: string | null;
  commnet: string;
  value: number;
};

const Ratings = (props: Props) => {
  return <div>Ratings</div>;
};

export default Ratings;
